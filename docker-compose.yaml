version: '3'

services: 
    service-1: 
        init: true
        image: eurekarobotics/focal:rave-20200901
        container_name: dev-env
        stop_signal: SIGINT
        network_mode: host
        security_opt:
            - seccomp:unconfined
        volumes: 
            - .:/workspace/repo
        tty: true
        command: ["cat"]